import { Profile } from '../../model/profile/profile';
import { Solicitation } from '../../model/solicitation/solicitation';
export class MockAvals {

    public buildList(users: Profile[], solicitations: Solicitation[]) {
        return [];
    }
    public getAvaliations() {
        return [];
    }
}
